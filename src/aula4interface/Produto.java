package aula4interface;

public class Produto implements Ordenavel {
    @Override
    public int compareTo(Ordenavel outro) {
        // Implementação da comparação de produtos
    	return 1;
    }
}